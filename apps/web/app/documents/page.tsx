"use client";
import {useState} from 'react';
import {FileText,Upload,Search} from 'lucide-react';
export default function Documents(){const [name,setName]=useState('');const [files,setFiles]=useState<string[]>([]);function add(){if(name){setFiles(v=>[...v,name]);setName('')}}return <main className="main"><div className="top"><div><div className="eyebrow">Knowledge base</div><h1 className="title">Documents</h1><div className="subtitle">Prepare resume and reference material for grounded interview answers.</div></div></div><div className="grid"><section><div className="card hero"><div><FileText size={34}/><h2>Build your interview context</h2><p className="subtitle">The document workspace is ready for resume, project notes, job descriptions and study material.</p></div><div className="composer"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Document name"/><button className="primary" onClick={add}><Upload size={16}/> Add</button></div></div></section><aside className="card"><div className="eyebrow">Indexed files</div>{files.length===0?<p className="subtitle">No documents yet.</p>:<div className="list">{files.map(f=><div className="row" key={f}><span>{f}</span><Search size={15}/></div>)}</div>}</aside></div></main>}
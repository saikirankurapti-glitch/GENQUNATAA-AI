"use client";
import {useEffect,useState} from 'react';
import {ArrowLeft,BarChart3,Brain,ChevronRight,FileText,MessageSquare,Radio,RefreshCw} from 'lucide-react';
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000';
type Row={id:string;title:string;mode:string;created_at:string|null;question_count:number;note_count:number;average_confidence:number};
type Data={total_sessions:number;total_questions:number;total_notes:number;average_confidence:number;mode_counts:Record<string,number>;sessions:Row[]};
export default function History(){const [data,setData]=useState<Data|null>(null);const [selected,setSelected]=useState<Row|null>(null);const [detail,setDetail]=useState<any>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState('');
async function load(){setLoading(true);setError('');try{const r=await fetch(API+'/api/v1/sessions/analytics');const d=await r.json();if(!r.ok)throw Error(d.detail||'Unable to load session analytics');setData(d)}catch(e){setError(e instanceof Error?e.message:'Request failed')}finally{setLoading(false)}}
async function open(row:Row){setSelected(row);const r=await fetch(`${API}/api/v1/sessions/${row.id}/analytics`);if(r.ok)setDetail(await r.json())}
useEffect(()=>{load()},[]);
const pct=(data?.average_confidence||0)*100;
return <main className="main"><div className="top"><div><div className="eyebrow">Performance center</div><h1 className="title">Interview History</h1><div className="subtitle">Review every session, question, note and confidence signal in one place.</div></div><div className="actions"><button className="secondary" onClick={load}><RefreshCw size={15}/> Refresh</button><button className="secondary" onClick={()=>window.location.href='/'}><ArrowLeft size={15}/> Workspace</button></div></div>{error&&<div className="card" style={{marginBottom:18}}>{error}</div>}{loading?<div className="card">Loading analytics…</div>:data&&<><div className="stats"><div className="stat"><span className="label">Sessions</span><b>{data.total_sessions}</b></div><div className="stat"><span className="label">Questions</span><b>{data.total_questions}</b></div><div className="stat"><span className="label">Notes</span><b>{data.total_notes}</b></div><div className="stat"><span className="label">Avg confidence</span><b>{Math.round(pct)}%</b></div></div><div className="grid" style={{marginTop:20}}><section><div className="card"><div className="eyebrow"><BarChart3 size={14}/> Recent sessions</div>{data.sessions.length===0?<p className="muted">No interview sessions yet. Start a live or mock interview to build your history.</p>:data.sessions.map(row=><button key={row.id} className="row" style={{width:'100%',textAlign:'left',background:'transparent',border:0,cursor:'pointer',padding:'14px 0'}} onClick={()=>open(row)}><span><strong>{row.title}</strong><small className="muted">{row.mode} · {row.created_at?new Date(row.created_at).toLocaleString():'unknown date'}</small></span><span><b>{row.question_count}</b> questions · {Math.round(row.average_confidence*100)}% <ChevronRight size={15}/></span></button>)}</div></section><aside><div className="card"><div className="eyebrow">Confidence overview</div><div style={{fontSize:42,fontWeight:700}}>{Math.round(pct)}%</div><div style={{height:10,background:'#e7e7e7',borderRadius:99,overflow:'hidden',margin:'14px 0'}}><div style={{width:`${Math.min(100,pct)}%`,height:'100%',background:'currentColor'}}/></div><p className="subtitle">Average confidence across sessions with analyzed questions.</p><div className="eyebrow" style={{marginTop:24}}>Session modes</div>{Object.entries(data.mode_counts).map(([mode,count])=><div className="row" key={mode}><span>{mode}</span><b>{count}</b></div>)}</div></aside></div></>}{selected&&<div className="card" style={{marginTop:20}}><div className="eyebrow"><Brain size={14}/> {selected.title}</div>{detail?<><div className="stats"><div className="stat"><span className="label">Questions</span><b>{detail.question_count}</b></div><div className="stat"><span className="label">Notes</span><b>{detail.note_count}</b></div><div className="stat"><span className="label">Avg confidence</span><b>{Math.round(detail.average_confidence*100)}%</b></div><div className="stat"><span className="label">Strong answers</span><b>{detail.high_confidence_questions}</b></div></div>{detail.questions?.map((q:any)=><div className="answer" style={{marginTop:14}} key={q.id}><b>{q.question_type} · {Math.round((q.confidence||0)*100)}%</b><p>{q.transcript}</p><p>{q.answer}</p></div>)}{detail.notes?.map((n:any)=><div className="row" key={n.id}><span><FileText size={14}/> {n.content}</span></div>)}</>:<p>Loading session…</p>}</div>}<button className="secondary" style={{marginTop:20}} onClick={()=>window.location.href='/'}><ArrowLeft size={15}/> Back to workspace</button></main>}
